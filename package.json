{
  "name": "datalytics-elis",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend-python && uvicorn main:app --reload --port 3001"
  }
}
